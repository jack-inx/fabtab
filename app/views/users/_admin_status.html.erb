<%  if user.admin == false %>
<%= link_to "User",adminstatus_change_path(user.id),:remote => "true" %>
<% else %>
 <%= link_to "Admin",adminstatus_unchange_path(user.id),:remote => "true"%>
<% end %>
