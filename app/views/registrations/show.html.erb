<div class="container">
  <div class="content" style="margin-top: 50px">
		<div class='row'>
				<ul id="saved_ads_carousel">				
					<% @user_ads.each do |ad| %>  
						<li><img src="<%= ad.image_url %>" /></li>
					<% end %>
 				</ul>   
		</div>  
	</div>
</div>
